{
  "name": "knit-pattern",
  "version": "1.0.0",
  "description": "Knit pattern generator",
  "scripts": {
    "test": "jest",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.8.0"
  }
}
